name: test build

on:
  push:

jobs:
  build-plugin:
    name: build-plugin
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v2
        with:
          token: ${{ secrets.FYLR_BOT_PERSONAL_ACCESS_TOKEN }}
          submodules: true

      - name: Build plugin
        run: make
